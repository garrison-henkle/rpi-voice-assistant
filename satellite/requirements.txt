sounddevice==0.5.1
# tflite-runtime 2.14 (transitive via openwakeword) was compiled against
# numpy 1.x; numpy 2.x breaks `tflite_runtime.interpreter` with
# `AttributeError: _ARRAY_API not found`. Pin to 1.x.
# moonshine-voice transitively pulls onnxruntime which is also numpy 1.x.
# sounddevice 0.5.1 also still works fine with numpy 1.x.
numpy>=1.24,<2
openwakeword==0.6.0
requests==2.32.3
# Streaming STT. `moonshine-voice` ships a `Transcriber.create_stream(...)`
# that consumes audio chunks incrementally and emits line-based partial
# transcripts. We use the auto-downloaded `moonshine/base` ONNX model
# (~120 MB) and bake it into the image at build time.
moonshine-voice
